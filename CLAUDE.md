# CLAUDE.md

Repo-level instructions for Claude Code and any agent reading from this file.

## Agent skills

### Issue tracker

GitHub Issues at `pmossman/swutrade` — skills use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context. `CONTEXT.md` + `docs/adr/` at the repo root; the existing `docs/wiki/` is the deep dive. See `docs/agents/domain.md`.
