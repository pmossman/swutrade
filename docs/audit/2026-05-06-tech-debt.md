# Tech-debt + architecture audit — 2026-05-06

> Overnight autonomous audit by Claude. Goal: identify architectural improvements
> that make new feature work easier + safer, and consolidate user-facing
> inconsistencies. Findings prioritised into **autonomous-safe (🟢)**, **needs your
> review (🟡)**, and **architectural (🔴)** buckets.
>
> Companion file: [`2026-05-06-visual-changelog.md`](./2026-05-06-visual-changelog.md)
> — every commit that touches user-visible UX is logged there with revert instructions.

## Status

| Phase | Status | Notes |
|---|---|---|
| 1. Discovery (per-category audit) | ✅ done | 70 findings across A-N |
| 1.5. Meta-pass (root-cause grouping + prevention levers) | ✅ done | 8 root causes + lever table |
| 2. Prioritisation (🟢/🟡/🔴 bucketing) | ✅ done | 15 🟢 batches, 10 🟡 slices, 5 🔴 questions |
| 3. Execute autonomous-safe items | _in progress_ | hard caps: 6h elapsed / 20 commits. CI failures: get to green before continuing |
| 4. Morning briefing | _pending_ | this doc + visual changelog |

## Hard rules

- Never touch auth / payment / identity / session-security flows autonomously → always 🟡.
- Never run schema migrations autonomously → always 🟡.
- Every commit that changes user-visible UX gets a row in the visual changelog.
- `gh run list --branch beta --limit 1` before every push; if last CI red, do not stack new commits.
- 2 consecutive CI failures: stop stacking new commits and fix CI to green before continuing the plan (don't stop the audit).
- Never use `--no-verify` or any CI-bypass flags.

## Commits shipped this session

- `0d573cd` — docs: post-Phase-C drift cleanup — C1 (wiki/comment/roadmap drift) — _CI: pending_

---

## Phase 1 — Per-category findings

> Format per finding: **F-{ID}** _(title)_ — file:line — what's inconsistent or fragile — proposed action — bucket
>
> Bucket key: 🟢 autonomous-safe · 🟡 your review · 🔴 architectural

### A. Component pattern consistency

- **F-A1** — `src/components/ListRows.tsx:30-90` (RemoveButton) and `src/components/ClearAllButton.tsx:13-68` duplicate the two-tap-confirm pattern (timer refs, disarm cleanup, 3s timeout) — extract a shared `<ConfirmButton>` so adoption is one-liner. 🟢
- **F-A2** — `src/components/lists/WantsPanel.tsx:156-163` defines a custom EmptyState (centered text + title + body, py-10 px-6) divergent from canonical `<EmptyState>` (rounded-lg bg-space-800/40 border) — adopt canonical or merge variants. 🟢
- **F-A3** — `src/components/lists/AvailablePanel.tsx` likely has the same custom EmptyState as WantsPanel (verify and consolidate). 🟢
- **F-A4** — `src/components/SignalBuilderView.tsx:660-696` — local `EmptyState` function with py-4 text-sm + custom button styling, separate from canonical states.tsx component — adopt canonical or derive a variant. 🟢
- **F-A5** — `src/components/SessionSuggestComposer.tsx:184-199` and `src/components/SessionView.tsx:~600` render footer errors as raw `<div className="text-[11px] text-red-400">{error}</div>` — both should use `<ErrorState variant="banner">`. 🟢
- **F-A6** — `src/components/SessionTimelinePanel.tsx` renders errors as raw `text-red-400` div — same fix as F-A5; standardise across composer/timeline/trade-summary. 🟢
- **F-A7** — `src/components/Popover.tsx` + `src/components/FilterPopover.tsx` (custom Popover); `src/components/TradeSearchOverlay.tsx` (full-screen fixed overlay, no Radix base); `src/components/ListsDrawer.tsx` (ad-hoc fixed modal) — three overlay paradigms — unify on Radix Dialog or one shared overlay abstraction. 🟡
- **F-A8** — `src/components/TradeSearchOverlay.tsx:95-210` reimplements Dialog patterns (open/close animations, ESC handler) by hand — migrate to Radix Dialog for consistency with ReportProblemDialog / HandlePickerDialog / CardActionsDialog. 🟡
- **F-A9** — Three "edit" surfaces have divergent header chrome: SessionSuggestComposer (Cancel + Send), SignalBuilderView (kind toggle + guild selector), ListsDrawer (yet another) — share a base architecture for action-bar headers. 🟡
- **F-A10** — `src/components/PrimaryActionBar.tsx` is the canonical bottom bar (loading / error / sent states); session Confirm/Cancel/Unconfirm buttons live inline in SessionView with divergent styling — consolidate into one `<ActionBar>`. 🟡
- **F-A11** — `src/components/SessionSuggestions.tsx` Accept/Dismiss buttons use `disabled:opacity-50`; SessionView Confirm button uses different disabled chrome (full dark bg) — standardise disabled-state across action buttons (pick one of 40/50/60). 🟢

### B. State management (hooks, contexts, fetch+cache)

- **F-B1** — `src/hooks/useSession.ts:243-249` — `saveCards` rapid-click race acknowledged in code comment but NOT FIXED; two overlapping calls both optimistically update; first response overwrites second. Apply gen-counter pattern (used in useGuildMemberships) to detect stale canonical responses. 🟡
- **F-B2** — `src/hooks/useSession.ts:236-256` — `pollPausedRef` is documented as poll-vs-mutation contract only; rapid confirm/unconfirm clicks lack mutation-vs-mutation protection — extend gen-counter to all mutations (confirm, cancel, decline, claim). 🟡
- **F-B3** — `src/components/SessionView.tsx:242-255, 256-277` — Confirm and Cancel handlers fire without local concurrency guard — add per-button `inFlight` disable. 🟢
- **F-B4** — `src/hooks/useServerSync.ts:208-211` polls at 60s while session poll is 2.5s — document the polling covenant or unify cadences. 🟡
- **F-B5** — Three localStorage persistence paths: useAuth (signed-in hint), useTradeViewMode, useTutorial — extract to `usePersistedState` wrapper or one localStorage module. 🟡
- **F-B6** — `src/components/ProfileView.tsx:146-155` direct `fetch()` bypasses apiClient — switch to `apiGet()`. 🟢
- **F-B7** — `src/components/AutoBalanceBanner.tsx:~95-105` direct `fetch()` to `/api/user/{handle}` with bespoke error recovery — consolidate (probably extract a `useUserProfile(handle)` hook). 🟢
- **F-B8** — `src/components/SessionView.tsx:~595-610` direct `fetch()` POST to `/api/sessions/{id}/invite-handle` with hand-rolled response parsing — switch to `apiPost()`. 🟢
- **F-B9** — `src/components/TradeSummary.tsx:227-238` fire-and-forget `fetch()` to `/api/trades` swallows errors silently — at minimum add `.catch(console.error)`. 🟢
- **F-B10** — `src/components/ShareLiveTradeButton.tsx:41-55` failure path completes silently with no error UI — add toast/banner. 🟢
- **F-B11** — `src/hooks/usePriceData.ts:48` `catch (err: any)` accesses `err.message` without narrowing — use `err instanceof Error ? err.message : 'Unknown error'`. 🟢
- **F-B12** — `src/contexts/PricingContext.tsx` owns mutations (`setPercentage`, `setPriceMode`) violating "contexts read-through-only" architecture — move mutation surface into a hook wrapping `usePersistedState`. 🟡
- **F-B13** — `useMyTrades` and `useActiveSessions` both call `/api/me/sessions` and project differently — consider higher-level `useSessions` that caches raw and projects. 🟡
- **F-B14** — `src/services/livePrices.ts:87, 112, 142` reads/writes `window.localStorage` in three separate functions; manual TTL — use `readPersisted`/`writePersisted` from `src/persistence/index.ts` consistently. 🟢
- **F-B15** — `src/hooks/useGuildMemberships.ts:155-156` sessionStorage write wraps in try/catch but doesn't fall back if storage fails — add `hasStorageAvailable` check or document the silent-fail. 🟢

### C. Error / loading / empty UX

- **F-C1** — `src/components/SignalBuilderView.tsx:447` inline `<span className="text-gray-500 italic">Loading…</span>` — replace with `<LoadingState inline label="Loading…" />`. 🟢
- **F-C2** — `src/components/AppFooter.tsx:62` inline `<span className="text-gray-500 animate-pulse">Loading prices…</span>` — migrate to LoadingState. 🟢
- **F-C3** — `src/components/HandlePickerDialog.tsx` custom error `<p id="handle-picker-error" className="mt-2 text-[11px] text-red-300">` — use `<ErrorState variant="banner">`. 🟢
- **F-C4** — `src/components/SessionView.tsx:~600` invite-form error rendered raw; counterpart-edit notice at line ~453 is a cyan banner with no semantic link to error/alert system — adopt ErrorState; consider `<NoticeState>` variant for the cyan tier. 🟢
- **F-C5** — `src/components/ProfileView.tsx:~100` error `<span className="text-gray-400">{error}</span>` — migrate to ErrorState. 🟢
- **F-C6** — `src/components/CommunityView.tsx` mixes `<ErrorState>` with custom `<NoGuildsEmptyState />` subcomponent — unify empty-state rendering or parametrise canonical EmptyState better. 🟢
- **F-C7** — `src/components/ListView.tsx` filter-mismatch as raw `<span>{isAnyLoading ? 'Loading card data…' : 'No items in this shared list.'}</span>` — use canonical EmptyState/ErrorState. 🟢
- **F-C8** — `src/components/SettingsView.tsx` correctly uses `<ErrorState variant="banner">` but message templates drift across surfaces — write one canonical "Couldn't load {resource}. Try refreshing." pattern. 🟢
- **F-C9** — `src/components/TradesHistoryView.tsx` empty-state copy varies in voice between tabs (active vs settled) — establish empty-state copy guidelines and align. 🟢
- **F-C10** — `src/components/ReportProblemDialog.tsx:146-158` success state is custom emerald-bordered div with `role="status"` — promote to a `<SuccessState>` sibling of LoadingState/ErrorState in `src/components/ui/states.tsx`. 🟢

### D. Forms + confirmations

- **F-D1** — `src/components/SessionSuggestComposer.tsx:139` uses `window.confirm("Discard this suggestion?...")` — replace with a reusable `<ConfirmDialog>` (Radix). 🟢
- **F-D2** — `src/components/SessionView.tsx:268` and `:298` use two separate `window.confirm` for cancel and decline — both should use ConfirmDialog with parameterised copy. 🟢
- **F-D3** — `src/components/SignalBuilderView.tsx:155` `window.confirm('Discard this draft and return home?')` — same fix. 🟢
- **F-D4** — `src/components/MobileActionsKebab.tsx:50` `window.confirm('Clear all cards from both sides?')` while `ClearAllButton` uses two-tap-confirm for the same action — unify (probably promote ClearAllButton or ConfirmButton everywhere). 🟢
- **F-D5** — Disabled-state opacity drift: 40% (SessionSuggestComposer:173), 50% (SignalBuilderView:275, :298), 60% (others) all coexist — pick one (suggest 50%) and audit. 🟢
- **F-D6** — Submit button label progression inconsistent: "Sending… → Sent" (ReportProblemDialog:177), "Send suggestion → Sending…" (SessionSuggestComposer), "↶ Revert to this state → Sending…" (SessionTimelinePanel) — standardise template (`{verb} → {verb}…`). 🟢
- **F-D7** — `src/components/TradeSummary.tsx` uses "Saving…" for one button and "Sending…" for another with no consistent verb — establish canonical action labels and stick to one per intent. 🟢
- **F-D8** — `src/components/SettingsView.tsx:223` button labelled "Done" (no client-side validation visible; only server-side error after submit) — add field-level validation (handle-length hints, required markers). 🟡
- **F-D9** — Some submit buttons disable while pending; others show loading state without disabling — audit and standardise to "always disable while pending + show loading text". 🟢
- **F-D10** — `src/components/ListRows.tsx:30-90` two-tap-confirm is the only adoption of this pattern (besides ClearAllButton); the rest of the destructive actions (cancel, decline, discard, clear) still use `window.confirm` — adopt uniformly via shared `<ConfirmButton>` (see F-A1). 🟢

### E. API client / ActionResult adoption

- **F-E1** — `src/services/apiClient.ts:104` casts `data as T` without schema validation — wire-format drift unobserved until property access. Add JSDoc warning + flag hot paths for incremental Zod migration. 🟡
- **F-E2** — `src/services/apiClient.ts:55-57` reads both `body.detail` and `body.error` to support legacy endpoints — pick one wire shape and enforce. 🟡
- **F-E3** — Many endpoints return `{error}`, some return `{error, detail}`, some plain HTTP — failure() tolerates both but divergence is error-prone — enforce single error shape in API spec. 🟡
- **F-E4** — `src/components/HandlePickerDialog.tsx:138-149, 169-180, 182-194` three apiGet/apiPost with divergent error branching — refactor into one helper. 🟢
- **F-E5** — `src/components/ShareLiveTradeButton.tsx:47-51` doesn't check `result.data.id` non-null on success — add defensive check or Zod schema. 🟢
- **F-E6** — `src/hooks/usePriceData.ts:31-56` no retry on fetch failure; gives up on first error — add exponential backoff or retry-via-button. 🟡
- **F-E7** — `src/services/priceService.ts` likely uses direct fetch (line 57, 83) — wrap in apiGet or document why static-asset fetch is exempt. 🟢
- **F-E8** — `src/hooks/useGuildMemberships.ts:118-143` apiPost reason mapping vs comment about 409 disagree — clarify whether reason comes from body or HTTP status. 🟢

### F. Type safety

- **F-F1** — `src/components/ProfileView.tsx:165` `bestMatchForWant(synth as any, ...)` — extract Zod schema for profile.wants items and validate. 🟡
- **F-F2** — `src/hooks/usePriceData.ts:48` `catch (err: any)` — narrow with `instanceof Error`. 🟢 (overlap with F-B11)
- **F-F3** — `src/components/ProfileView.tsx:169, 180` unsafe cast after `.filter(Boolean)` — use `satisfies` or runtime assertion. 🟢
- **F-F4** — `src/components/SessionView.tsx:1542` `as CardVariant` cast on partial object — define `PartialCardVariant` or validate with Zod. 🟢
- **F-F5** — `src/services/apiClient.ts:86, 104` `as Record<string, unknown>` after `.json().catch(() => null)` — add type guard for the null case. 🟢
- **F-F6** — `src/hooks/useSession.ts:267-270` `apiGet<{session?, preview?}>` doesn't validate at-least-one — add Zod discriminant. 🟢
- **F-F7** — `src/components/HandlePickerDialog.tsx:138-149` apiGet typed but unvalidated; if API returns flat shape code crashes at `.user.handle` — Zod schema at boundary. 🟢
- **F-F8** — `src/services/apiClient.ts` request signature has optional `schema?` — make it required for new call sites; doc note + a lint allowlist for legacy. 🟡
- **F-F9** — `src/hooks/useFavorites.ts` apiPost/apiDelete no schema; if server shape drifts caller applies stale data silently — incremental Zod adoption. 🟡
- **F-F10** — `src/hooks/useServerSync.ts:145-146` `as typeof wants.items` cast hides shape mismatches — validate with Zod before cast. 🟡

### G. Render performance

- **F-G1** — `src/contexts/PricingContext.tsx:53` — context value object `{percentage, setPercentage, ...}` recreated every render; consumers (TradeBalance, TradeSummary, AutoBalanceBanner) re-render unnecessarily — wrap in `useMemo`. 🟢
- **F-G2** — `src/components/lists/AvailablePanel.tsx:98` — binder list maps 500-2000 rows; `<AvailableRow>` not memoised, recomputes per parent render — wrap in `React.memo`. 🟢
- **F-G3** — `src/components/SessionView.tsx:140-144` — `incomingSuggestions`, `outgoingSuggestions`, `revertSuggestions` are `.filter()` calls in render body — wrap in `useMemo`. 🟢
- **F-G4** — `src/components/SessionView.tsx:161-165` — `breadcrumbs` memo depends on full `session` object; revisit dep tracking to avoid invalidation churn. 🟢
- **F-G5** — `src/components/CardResultsGrid.tsx:72-90` — virtualizer estimates row heights in callback without caching; mount latency on 1000+ rows worth profiling. 🟡

### H. Mobile / desktop parity

- **F-H1** — `src/components/PrimaryActionBar.tsx:64-96` — bottom-pinned bar may overlap visualViewport when keyboard opens; add `env(safe-area-inset-bottom)` guard. 🟢
- **F-H2** — `src/components/ui/AppHeader.tsx:67` — header `gap-3` at all widths; breadcrumbs may truncate on narrow mobile — add explicit mobile-aware rendering. 🟡
- **F-H3** — `src/components/FilterPopover.tsx:49` — popover panel `w-[min(320px,calc(100vw-2rem))]` smart but takes 90% width on phones <360px — verify panel UX on small screens. 🟢
- **F-H4** — `src/components/SessionSuggestComposer.tsx:150-200` — full card-picker overlay on mobile landscape may not fit; no explicit landscape handling. 🟡
- **F-H5** — `src/components/TradeImageModal.tsx:191` — long-press hint shown only on touch; desktop users see no affordance for save-via-right-click — add desktop hover hint. 🟢
- **F-H6** — `src/components/PanelDivider.tsx:50` — resize handle hit-zone is 3px (visual height); apply `hit-area-44` pseudo-element pattern. 🟢

### I. Palette / spacing tokens

- **F-I1** — `src/components/AccountMenu.tsx:73` and `src/components/SettingsView.tsx:1062` — Discord brand color `#5865F2` hardcoded in two places — extract to `--color-discord` token in `index.css` / `tailwind.config.js`. 🟢
- **F-I2** — `src/components/CardTile.tsx:41-43` and `src/components/FamilyRow.tsx:56-58` — accent border shadows hardcoded RGBA — reference theme tokens (`--shadow-glow-gold` etc.) defined at `index.css:47-58`. 🟢
- **F-I3** — `src/components/ProfileView.tsx:510` — inline `style={{ fontSize: 12, lineHeight: 1 }}` on priority star — use `text-[12px] leading-none`. 🟢
- **F-I4** — Border-radius scale undocumented: buttons use `rounded-lg`, cards/panels `rounded-xl`, inline badges `rounded-md` — add intent comment to `tailwind.config.js` (md=controls, lg=buttons, xl=containers). 🟢
- **F-I5** — Spacing drift: `px-3` vs `px-4` vs `px-2.5` for similar list rows across `ListRows.tsx`, `SessionView.tsx`, etc. — unify with a `row-padding` utility class. 🟡

### J. Accessibility

- **F-J1** — `src/components/ui/NumberStepper.tsx:107-147` — increment/decrement buttons are `w-6 h-6` (24px) or `w-5 h-5` (20px); below 44×44 minimum on mobile — add `hit-area-44` pseudo-element. 🟢
- **F-J2** — `src/components/SessionSuggestComposer.tsx:66-100` — locked-hint and error region not `aria-live="polite" role="status"`; screen readers won't announce updates — wrap region. 🟢
- **F-J3** — `src/components/HandlePickerDialog.tsx:205-222` and `src/components/CardActionsDialog.tsx:64` — `aria-describedby={undefined}` set explicitly without describing element — drop the prop or wire to a description. 🟢
- **F-J4** — `src/components/ui/CardThumb.tsx:64` — card image `<img>` likely has no `alt`; should be `alt={`${card.name} card art`}` for screen-reader access. 🟢
- **F-J5** — `src/components/ProfileView.tsx:264` and `src/components/TradeImageModal.tsx:121` — images may lack `alt` text — audit + add contextual alt. 🟢
- **F-J6** — `src/components/SettingsView.tsx:425-430` — form inputs may lack `<label htmlFor=>` association — audit form-input/label pairs across the app. 🟡

### K. Dead code

- **F-K1** — `api/trades.ts:14-19` comments still reference deleted proposal flow — remove the stale comment. 🟢
- **F-K2** — `lib/discordMessages.ts:38-41` comment about being formerly named proposalMessages.ts is historical noise — trim. 🟢
- **F-K3** — `api/bot.ts:1147-1148` "Remove once deployed DMs have rolled over to communicationPref" — Phase C done; remove. 🟢
- **F-K4** — `ROADMAP.md:77` says "trade proposals" in DM-defaults description — update to session-lifecycle. 🟢
- **F-K5** — `ROADMAP.md:96-102` "Phase 5a — Trade proposal composer / private threads" — supersede with Phase 5b unified primitive. 🟢
- **F-K6** — `docs/wiki/j-infra.md:31` claims journal has 19 entries through 0018 — reality is 30 through 0029 — update. 🟢
- **F-K7** — `lib/schema.ts:18-22` "Upgrade path: ghost → real" reads like TODO; the flow is implemented — clarify. 🟢

### L. Test coverage

- **F-L1** — `api/trades.ts` (post-C1c GET/POST personal saved-trades) has no integration test — create `tests/api/trades.test.ts`. 🟢
- **F-L2** — `api/search.ts` (TCGPlayer search proxy) has no test — create `tests/api/search.test.ts`. 🟢
- **F-L3** — Wiki references `e2e/session-collaboration.auth.spec.ts` but the file does not exist — update wiki or recreate spec. 🟢
- **F-L4** — Session DM toggles (`dmSessionInvited`, etc.) not exercised in e2e — add coverage. 🟡
- **F-L5** — `api/sessions.ts:66/74` declineSession + pingSession take an `options` param always passed `{}` — verify whether this is dead-code or partial implementation. 🟢

### M. Schema ↔ API ↔ UI drift

- **F-M1** — `src/types/index.ts:10-30` defines `CardVariant` interface, never imported by components; instead components use inferred types or local copies — unify by exporting `z.infer<typeof CardVariantSchema>` from persistence. 🟡
- **F-M2** — API handlers in `api/sessions.ts` and `api/me.ts` construct `AvailableItem`/`WantsItem` shapes without validating against Zod schema — add validation at API boundary. 🟡
- **F-M3** — `api/search.ts:2-18` redefines `CardData`/`CardGroup` instead of importing from `src/types` — unify or document. 🟢
- **F-M4** — `trade_sessions.status` enum lacks Zod runtime validation in response payloads — add. 🟡
- **F-M5** — `lib/prefsRegistry.ts:COMMUNICATION_PREF_OPTIONS` duplicates the schema enum — export from one source. 🟢

### N. Wiki / context.md drift

- **F-N1** — `docs/wiki/j-infra.md:38-40` says vercel.json has no `crons` field — false; signal-refresh cron is at lines 54-58 — fix. 🟢
- **F-N2** — `docs/wiki/a-sessions.md` File map references `e2e/session-collaboration.auth.spec.ts` which doesn't exist — update. 🟢 (overlap with F-L3)
- **F-N3** — `api/context.md:8-11` documents `/api/me/settings` as transitional alias for `/api/me/prefs` — confirm + add deprecation timeline or delete. 🟢
- **F-N4** — `docs/wiki/README.md` index marks B as "_retired_" but offers no pointer to where the proposal-era info lives now — add a link to ROADMAP Phase 5b or a brief stub. 🟢
- **F-N5** — `lib/context.md:41-42` historical context about proposalMessages.ts → discordMessages.ts is now noise (proposals don't exist) — trim. 🟢

---

## Phase 1.5 — Meta-pass (root-cause groupings + prevention levers)

### Recurring root causes

The 70+ findings cluster into ~8 root causes. Fixing the cause once eliminates the category.

#### MP-1 · No "small primitive" component library
The biggest pattern across A/C/D: components that should share a base instead reach for fresh JSX every time. `LoadingState`, `ErrorState`, `EmptyState` exist but are unevenly adopted (F-A2 through F-A6, F-C1 through F-C7); button-disabled chrome drifts (F-A11, F-D5); `<ConfirmButton>` doesn't exist so two-tap-confirm is duplicated (F-A1) and `window.confirm` is the lazy fallback in 4+ places (F-D1 through F-D4).

**Lever:** ship `src/components/ui/` with `LoadingState`, `ErrorState`, `EmptyState`, `SuccessState`, `ConfirmButton`, `ConfirmDialog`, `Pill`, `Banner`. The first three exist; the rest don't. **Each canonical primitive ships with one ESLint rule (or codebase comment) saying "don't roll your own."** Consolidating the duplicates into adoption commits forces the rest of the app to converge.

#### MP-2 · No fetch convention enforcement
`apiClient.ts` exists with the right shape (`ActionResult<T>`, status→reason mapping) but several components reach past it for direct `fetch()` (F-B6 through F-B9, F-E7) — and no rule prevents the next person from doing it again. Wire-format drift goes unnoticed because schema validation is opt-in (F-E1, F-F8).

**Lever:** (a) make `schema:` required on apiGet/apiPost/etc. for new call sites; (b) add an ESLint custom rule banning `fetch(` outside `src/services/apiClient.ts` and `src/services/livePrices.ts`; (c) write the missing `apiPatch`/`apiDelete` if any are missing; (d) Zod-schema the top-N hot paths incrementally — three this week, three next.

#### MP-3 · Race conditions hidden behind a single mutex pattern that doesn't scale
`mutationInFlightRef` works for poll-vs-mutation but fails for mutation-vs-mutation (F-B1, F-B2, F-B3). The wiki teaches the pattern as if it's complete; in practice there are at least three places (saveCards, confirm/cancel, suggest) where a fast double-click can produce stale state.

**Lever:** generalise to a per-session or per-mutation gen-counter that tags each request with a generation and rejects stale completions. `useGuildMemberships` already does this; promote it to a shared `useMutation` hook with the gen-counter built in. Document in `architecture.md`.

#### MP-4 · Two parallel sources of truth for several types
`CardVariant` exists twice (F-M1); `WantsItem`/`AvailableItem` API shapes don't validate against Zod (F-M2); search redefines its own card types (F-M3); the `communicationPref` enum is duplicated between schema and registry (F-M5). Each duplicate is one rename away from a runtime crash.

**Lever:** establish "schema is source of truth, types flow out via $inferSelect/$inferInsert + Zod schemas" — and ban hand-rolled parallel types via a comment-policy enforced on PR review (or a custom lint). The card-types in `src/types/index.ts` and `src/persistence/schemas.ts` should consolidate.

#### MP-5 · Confirmation UX is three-headed
`window.confirm` (F-D1-D4), two-tap-confirm (F-A1, ListRows + ClearAllButton), and Radix Dialog (HandlePickerDialog) all coexist for the same job. Beta users will eventually report different feel between equivalent destructive actions — and we'll have to retrofit again.

**Lever:** pick one canonical pattern per scope: **two-tap-confirm for in-row destructive** (cheap, low-friction); **`<ConfirmDialog>` for cross-cutting destructive** (more friction for higher stakes). `window.confirm` banned outright. Document in a new `docs/wiki/k-ui-primitives.md`.

#### MP-6 · Dead-code cruft from each rename
Phase C did a clean job overall but left ~15 stale comments / wiki notes / migration-cruft items (F-K1-K7, F-N1-N5). This is the same shape as the pref-registry residue from earlier consolidations. Each rename leaves a fingerprint we never re-grep.

**Lever:** add a one-liner to `feedback_workflow` memory: "after a rename or deletion slice, grep the codebase for the old name + its docs and clean refs in the same commit." Or better, ship a cleanup script (`scripts/cleanup-rename-residue.mjs <old-name>`) that does the find + echoes hits.

#### MP-7 · `localStorage` access scattered
F-B5, F-B14, F-B15: three or four parallel localStorage paths with handwritten try/catch and TTL handling — exactly the seams where the Safari-private-mode bugs hide. There's a `src/persistence/index.ts` already; not everyone uses it.

**Lever:** make `usePersistedState<T>(key, initial, schema?)` the canonical path; `localStorage` outside that file becomes a lint warning. Migrate the four offenders.

#### MP-8 · Visual-token discipline is convention-only
F-I1 (Discord brand color in two files), F-I2 (shadow RGBA hardcoded), F-I3 (inline style), F-I4 (rounded scale undocumented) — the palette memory says "pick from CSS-header table, don't inline hex" but nothing enforces it.

**Lever:** ship `eslint-plugin-tailwindcss` with our token allowlist; ban arbitrary `bg-[#…]` / `text-[#…]` / `shadow-[…]` outside `index.css` + `tailwind.config.js`. Fix the half-dozen offenders.

### Prevention levers — summary table

| # | Lever | Effort | Payback |
|---|-------|--------|---------|
| MP-1 | Ship full UI-primitive set + adopt across app | M (4-6 small commits) | every future component lands consistent for free |
| MP-2 | Lint-ban raw fetch + require schema on new call sites | S (1 commit) | no more direct-fetch creep |
| MP-3 | Promote gen-counter into `useMutation` + adopt | M (3-4 commits) | rapid-click race class extinguished |
| MP-4 | Consolidate card types to one source + Zod boundary | M (3-4 commits) | rename-driven runtime crashes prevented |
| MP-5 | Canonical confirm patterns (in-row vs dialog), ban window.confirm | S (2 commits) | destructive UX coherence |
| MP-6 | Memory note + optional cleanup-script | S (1 doc commit) | cleaner future rename slices |
| MP-7 | Standardise on `usePersistedState` + lint warn | S (1-2 commits) | localStorage failure modes contained |
| MP-8 | Tailwind lint plugin + token allowlist | S (1 commit) | palette discipline enforced |

### Library opportunities (free/open-source only)

- **`zod` (already a dep)** — the right tool for runtime schemas at the API/storage boundary; needs adoption push, not a new dep.
- **`eslint-plugin-react-hooks` (already a dep)** — already enforces exhaustive-deps; verify other rules in the plugin (set-state-in-effect, refs-in-render) aren't disabled.
- **`eslint-plugin-tailwindcss`** — would catch most of MP-8 automatically. Free, MIT, low-risk dep.
- **`@tanstack/react-query`** — would replace ~half the bespoke hook+cache+mutex+poll pattern with a battle-tested implementation. **🔴 Architectural** — bigger discussion, not autonomous; flagged for morning.
- **`vaul` or `@radix-ui/react-dialog`** — already using Radix Dialog in some places; if we're consolidating overlays (MP-5 / F-A7-A9), going all-in on Radix's full primitive set is the path. Already a dep; just adoption.
- **No new deps recommended for tonight's work.** Every prevention lever above can be done with the current dep tree + ESLint rules.

### Bigger architectural questions (🔴, for morning discussion)

- **Q1 — Adopt @tanstack/react-query?** It would obsolete the hand-rolled cache/mutex pattern across `useSession`, `useMyTrades`, `useActiveSessions`, `useTradeDetail`, etc. Big migration, big payoff. Risk: a year-long refactor commitment.
- **Q2 — Migrate fully off ad-hoc overlay paradigms onto Radix?** TradeSearchOverlay, ListsDrawer, FilterPopover all reimplement Dialog/Drawer/Popover. Migration sized at ~5-10 commits but invasive UX-wise.
- **Q3 — Server response shape standardisation.** Some endpoints return `{error}`, some `{error, detail}`, some plain status. Pick one (e.g., RFC 7807 problem-details, or simple `{ok, data?, error?}`) and migrate. Touches every endpoint + apiClient.

## Phase 2 — Prioritised work plan

### 🟢 Autonomous-safe (will ship tonight)

In commit order, batched by theme. Each batch ~one commit.

- **C1 · Wiki + comment cleanup** — F-K1, F-K2, F-K3, F-K4, F-K5, F-K6, F-K7, F-N1, F-N3, F-N4, F-N5. Mechanical.
- **C2 · useMemo perf wins** — F-G1 (PricingContext value memo), F-G3 (SessionView filter memos). Behaviour-identical perf.
- **C3 · Quick a11y batch** — F-J4 (CardThumb alt), F-J3 (aria-describedby on dialogs), F-J1 (NumberStepper hit-area-44). Tiny but high-value.
- **C4 · Typed catches + unsafe-cast cleanups** — F-B11, F-F2 (overlap), F-F3, F-F5. Safer error paths.
- **C5 · `<SuccessState>` primitive** — F-C10. New addition to states.tsx; refactor ReportProblemDialog success card onto it.
- **C6 · `<ConfirmButton>` primitive + adoption** — F-A1, F-A11, F-D4, F-D5, F-D9, F-D10. Extract two-tap pattern from ListRows + ClearAllButton; consolidate disabled-state opacity to 50%.
- **C7 · `<ConfirmDialog>` primitive + window.confirm migration** — F-D1, F-D2, F-D3 (and incidentally F-D4 already covered by C6). Replace 4 window.confirm calls.
- **C8 · Inline loading text → LoadingState** — F-C1, F-C2. Tiny visual touch (logged).
- **C9 · Inline error text → ErrorState** — F-A5, F-A6, F-C3, F-C5, F-C7. Logged in visual changelog.
- **C10 · `React.memo` AvailableRow** — F-G2. Perf, no visual change.
- **C11 · fetch → apiClient migrations** — F-B6, F-B7, F-B8, F-B9, F-B10. Five small commits or one batched depending on size.
- **C12 · Palette token: Discord brand colour** — F-I1. Trivial visual change.
- **C13 · aria-live region in SuggestComposer** — F-J2. Screen-reader UX win.
- **C14 · Custom EmptyState consolidation** — F-A2, F-A3, F-A4, F-C6. Logged.
- **C15 · localStorage standardisation** — F-B5, F-B14, F-B15. Migrate to usePersistedState/persistence helpers.

### 🟡 For your review (slices ready for morning)

Sized for one sitting each. Need design judgment, not just mechanical fix.

- **R1 · Generalise gen-counter into `useMutation`** (MP-3) — Lift the pattern from `useGuildMemberships` into `src/hooks/useMutation.ts` and migrate `useSession`'s `saveCards`, `confirm`, `cancel`, `decline`, `claim`, `suggest` to it. Closes F-B1, F-B2 race classes.
- **R2 · Lint-ban raw `fetch()` + require Zod schema on new apiClient calls** (MP-2) — One ESLint rule + a `.eslintrc` overlay. Migrate the remaining direct-fetch sites (after C11 ships, this becomes a guard rail).
- **R3 · Card-type single-source-of-truth** (MP-4, F-M1, F-M2, F-M3, F-M5) — Consolidate `CardVariant` definitions; add Zod validation at API boundary. Touches src/types, src/persistence/schemas, several hooks.
- **R4 · Polling cadence covenant** (F-B4) — Document why useServerSync polls 60s vs session's 2.5s; align or codify; add to `architecture.md`.
- **R5 · Overlay paradigm consolidation** (F-A7, F-A8, F-A9, MP-5 part 2) — Migrate TradeSearchOverlay, ListsDrawer's drawer logic to Radix Dialog/Drawer; bigger UX touch — wants your eyes.
- **R6 · `<ActionBar>` consolidation** (F-A10) — One sticky-bottom bar component for trade builder, sessions, and edit composers.
- **R7 · API error-shape unification** (F-E2, F-E3) — Pick `{error, code?, detail?}` (or RFC 7807); migrate every endpoint + apiClient.
- **R8 · Tests for `api/trades.ts` + `api/search.ts`** (F-L1, F-L2). Just integration tests; safe but I want you to confirm the test coverage gates we want.
- **R9 · `<NoticeState>` for non-error cyan banners** (F-C4 second half) — A new variant for "informational" tier (counterpart-edit notice etc.). Requires copy/UX call.
- **R10 · Settings field-level validation** (F-D8). Pure UX work; deserves a sketch.

### 🔴 Architectural (questions for discussion)

- **Q1 · Adopt @tanstack/react-query?** Replaces hand-rolled cache/mutex/poll across all hooks. Year-long-feeling commitment but likely net-positive. Need your call on appetite + timeline.
- **Q2 · Standardise overlay/dialog/drawer on Radix end-to-end?** (Subset of R5) — But also: do we need a generic `<Sheet>` primitive on top of Radix for our app's specific patterns?
- **Q3 · Backend response-shape standard.** Pick {ok, data, error} discriminated union or RFC 7807 problem-details. Touches every endpoint.
- **Q4 · Add `eslint-plugin-tailwindcss` with token allowlist?** Would close MP-8 with one config commit. Risk: lots of small fixes lighting up at first.
- **Q5 · Should `<ConfirmDialog>` and the two-tap-confirm pattern (`<ConfirmButton>`) coexist, or pick one?** I'm leaning "both, by stakes": in-row two-tap, cross-cutting dialog. Want your read.

---

## Phase 4 — Morning briefing summary

_pending — written at the end of the session_
